name: Keep Render App Awake

on:
  schedule:
    - cron: '*/10 * * * *' # Chạy mỗi 10 phút
  workflow_dispatch: # Cho phép chạy thủ công trên giao diện GitHub Actions

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: curl -s https://tnt-pccc.onrender.com > /dev/null
